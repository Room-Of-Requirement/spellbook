
name: Deploy
on:
  workflow_dispatch:
  push:
    branches:
    - main

jobs:
  push:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2.3.4
      with:
        fetch-depth: 0
    
    - uses: jmmaloney4/gomplate-action@main
