---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: uptimerobot-operator
  namespace: network
spec:
  interval: 1m
  chart:
    spec:
      chart: uptimerobot-operator
      version: 0.3.0
      sourceRef:
        kind: HelmRepository
        name: uptimerobot-operator
        namespace: flux-system
      interval: 1m
  values:
    uptimeRobotApiKey: ${UPTIME_ROBOT_API_KEY}
