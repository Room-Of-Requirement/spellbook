apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ipfs
  namespace: ipfs
spec:
  interval: 1m
  chart:
    spec:
      chart: basic
      version: 0.1.0
      sourceRef:
        kind: HelmRepository
        name: jmmaloney4
        namespace: flux-system
      interval: 1m
  values:
    env:
      TZ: "America/Chicago"
    image:
      repository: docker.io/ipfs/kubo
      tag: v0.19.1@sha256:a102467c6611fc372d8b6f9549081e24b892363842a83653ab1de810d39a1823
    controller:
      annotations:
        # backup.velero.io/backup-volumes: lfs-basic-data
    persistence:
      data:
        enabled: true
        size: 700Gi
        accessMode: ReadWriteMany
        storageClass: cephfs
        retain: true
    service:
      main:
        enabled: true
        ports:
          http:
            port: 8080
    ingress:
      main:
        enabled: false
        # hosts:
        # - host: ipfs.roomofrequirement.xyz
        #   paths:
        #   - path: "/"
        # annotations:
        #   nginx.ingress.kubernetes.io/proxy-body-size: "0"
        #   cert-manager.io/cluster-issuer: "letsencrypt-staging"
        #   nginx.ingress.kubernetes.io/whitelist-source-range: "10.0.0.0/8,172.10.0.1,192.168.1.0/24"
        # tls:
        # - secretName: lfs-cert
        #   hosts:
        #   - ipfs.roomofrequirement.xyz
