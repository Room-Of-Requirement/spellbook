apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: rclone
spec:
  values:
    to: "ceph:music"
    volumes:
    - name: data
      persistentVolumeClaim:
        claimName: roon-music
