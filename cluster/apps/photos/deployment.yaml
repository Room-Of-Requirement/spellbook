apiVersion: apps/v1
kind: Deployment
metadata:
  name: icloudpd
  labels:
    app: icloudpd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: icloudpd
  template:
    metadata:
      labels:
        app: icloudpd
    spec:
      containers:
        - name: icloudpd
          image: ghcr.io/jmmaloney4/icloudpd:main@sha256:ab53a13e9339f4cbf003744818ce68e781490501d2cc967a9707b62097f295cb
          command: ["/usr/bin/sleep", "365d"]
          volumeMounts:
            - mountPath: /data
              name: data
          env:
            - name: ICLOUD_EMAIL
              value: ${ICLOUD_EMAIL}
            - name: ICLOUD_PASSWORD
              value: ${ICLOUD_PASSWORD}
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: photos-downloads-pvc
