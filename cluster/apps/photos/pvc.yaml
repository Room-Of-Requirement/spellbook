apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photos-downloads-pvc
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  storageClassName: cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
