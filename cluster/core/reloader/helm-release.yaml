---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: reloader
  namespace: system
spec:
  interval: 1m
  chart:
    spec:
      # renovate: registryUrl=https://nvidia.github.io/gpu-feature-discovery
      chart: reloader
      version: v0.0.90
      sourceRef:
        kind: HelmRepository
        name: stakater
        namespace: flux-system
      interval: 1m
  values:
    nfd:
      deploy: false
