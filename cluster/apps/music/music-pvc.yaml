apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: music-pvc
  namespace: music
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  storageClassName: cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi
