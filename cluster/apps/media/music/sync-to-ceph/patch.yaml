apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rclone
spec:
  values:
    to: "ceph:music"
    from: "/data"
    volumes:
    - name: data
      persistentVolumeClaim:
        claimName: music-library-pvc
