apiVersion: apps/v1
kind: Deployment
metadata:
  name: icloudpd
  labels:
    app: icloudpd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: icloudpd
  template:
    metadata:
      labels:
        app: icloudpd
    spec:
      containers:
        - name: icloudpd
          image: ghcr.io/jmmaloney4/icloudpd:main@sha256:96948b16395928f19acf86062b3d620163ea1a08b68e12db71de5d91b32ebbee
          command: ["/usr/bin/sleep", "365d"]
          volumeMounts:
            - mountPath: /data
              name: data
          env:
            - name: ICLOUD_EMAIL
              value: ${ICLOUD_EMAIL}
            - name: ICLOUD_PASSWORD
              value: ${ICLOUD_PASSWORD}
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: photos-downloads-pvc
